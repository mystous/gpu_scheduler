% SQUAD --- IEEE (IEEEtran journal) format. Compile with XeLaTeX (kotex 한글).
% NOTE: For the official IEEE Access submission, replace the documentclass with
%   \documentclass{ieeeaccess}
% and add \history{...}\doi{...}\corresp{...}\address[n]{...} in the front matter.
\documentclass[journal]{IEEEtran}

\usepackage{kotex}            % Korean (XeLaTeX)
\usepackage{graphicx}
\usepackage{amsmath,amssymb,amsfonts}
\usepackage{amsthm}
\usepackage{mathrsfs}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{xcolor}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{cite}
\usepackage[hidelinks]{hyperref}

\newtheorem{theorem}{Theorem}
\newtheorem{proposition}[theorem]{Proposition}
\newtheorem{definition}{Definition}
\newtheorem{remark}{Remark}

\begin{document}

\title{SQUAD: Scheduling Queue Unblocking and Allocation Defragmentation for Gang Scheduling}

\author{Kyunam~Cho,
        YoungHwan~Jin,
        and~Heonchang~Yu%
\thanks{K. Cho and H. Yu are with the Department of Computer Science and Engineering, Korea University, Seoul, Republic of Korea (e-mail: mystous@korea.ac.kr; yuhc@korea.ac.kr).}%
\thanks{K. Cho is also with LG Uplus Corp., Seoul, Republic of Korea, and the School of Business, Ewha Womans University, Seoul, Republic of Korea.}%
\thanks{Y. Jin is with LG Uplus Corp., Seoul, Republic of Korea (e-mail: yhjin@korea.ac.kr).}%
\thanks{Corresponding author: Heonchang Yu.}}

\maketitle

\begin{abstract}
하드웨어 가속기 또는 보조 프로세서의 대표적 예인 데이터센터 GPU는 상당한 비용에도 불구하고 막대한 연산 능력을 제공하는, AI 연구에 필수적인 하드웨어 자원이다. 트랜스포머 기반 대규모 언어 모델(LLM)의 광범위한 확산과 함께 그 중요성은 더욱 커졌으며, LLM 학습에는 수십에서 수백 장의 NVIDIA 데이터센터 GPU가 요구되는 경우가 많다. 따라서 GPU 자원의 효율적인 할당과 관리는 매우 중요하다. 스케줄링 문제는 고전적인 NP-hard 조합 최적화 문제로, 실행 시간을 알 수 없는 작업들을 유한한 GPU 집합에 매핑하는 문제는 다항 시간 안에 최적으로 풀 수 없다. 기존 스케줄러들은 일반적으로 특정 가정 아래에서 단일 지표(예: 처리량, 공정성, 마감 기한 준수)를 최적화하지만, 작업 대기 큐 선두에서 발생하는 HOL 블로킹(head-of-line blocking, 선두 차단)으로 인한 기아(starvation)는 정책 계층과 무관하게 잔존한다. 프로덕션 GPU 트레이스 분석은 이러한 기아가 드물지 않게 발생함을 보여준다. SQUAD(Scheduling Queue Unblocking and Allocation Defragmentation for Gang Scheduling)는 또 하나의 스케줄러를 제안하는 대신, 누적·이력 상태 지표에 의존하지 않는 광범위한 기반(baseline) 스케줄러들을 보완·강화하는 전처리 메커니즘을 제시한다. 핵심은 기아 방지(starvation-free) 큐 조정 알고리즘(SFQA)으로, 대기 시간과 자원 요구량을 모니터링하여 HOL 블로킹을 감지하고 기아를 방지하도록 큐를 재정렬한다. 나아가 큐잉 이론에 근거해 튜닝 노브를 제거한 무튜닝(zero-knob) 변형 sfqa-auto는 클러스터 부하 $\rho$로부터 공정성 임계 $\sigma^*=1/(1-\rho)$를 직접 유도하여, 워크로드의 시간 스케일이 변해도 별도 재튜닝 없이 효율과 공정성의 균형점을 유지한다. 우리는 제안 기법을 두 시뮬레이터로 평가했다. 분포별 합성 트레이스 위의 이산 시간 시뮬레이터에서 SFQA는 packing·round-robin·MCTS 등 다양한 기반 스케줄러와 결합해 총 소요 시간을 약 7.8--14.98\% 단축했고, Philly 트레이스 111k 작업 규모의 이산 사건 시뮬레이터(256--1024 GPU, 단일·이종 클러스터)에서 sfqa-auto는 FIFO 대비 중앙값 대기를 50--61\% 단축하면서 하위 1\% 공정성을 유지하는 --- 빠르면서 공정한 --- 유일한 경량 정책이었다. SQUAD는 스케줄러 계층과 무관하게 그 하부에서 동작하며 핵심 정책을 그대로 유지하므로, 안정성을 해치지 않으면서 기존 GPU 클러스터 관리자에 통합되어 활용도를 높일 수 있다.
\end{abstract}

\begin{IEEEkeywords}
Head-of-line blocking, GPU scheduling, starvation-free queue adjustment, queue reordering, fragmentation, gang scheduling, datacenter GPU.
\end{IEEEkeywords}

\section{Introduction}\label{sec1}
GPU 서버에서 학습 또는 추론 작업을 스케줄링하는 일은 시스템 수준 최적화의 핵심 요소다. 서버의 GPU 할당률과 사용률은 운영 비용과 연구 비용에 직접적인 영향을 미치는 중요한 운영 지표다. 더욱이 GPU 자원은 연구·사업 조직에 본질적으로 한정되어 있어, 최적의 작업 스케줄링을 위한 연구가 활발히 이루어져 왔다. 일부 조직은 고대역폭 인터커넥트로 연결된 수백~수천 장 GPU의 전용 클러스터인 ``하이퍼클러스터''를 구성한다. 이 접근은 비용을 끌어올릴 뿐 아니라 확장성의 한계를 부과하고 클러스터 간 자원 단편화(fragmentation)를 유발한다\cite{athlur2022varuna}. 마찬가지로, 프로덕션 트레이스 분석에 따르면 공유 환경에서의 부분(fractional) GPU 할당은 심각한 단편화를 초래하여 수백 장의 GPU가 유휴 상태로 남아 신규 작업에 할당되지 못하게 만든다\cite{USENIX288717}. 이러한 발견들은 모델 규모의 증가와 다양한 워크로드 속에서도 GPU 자원을 효과적으로 관리할 수 있는 스케줄러와 단편화 해소 메커니즘 설계의 중요성을 잘 보여준다. 그러나 대부분의 연구가 특정 조건이나 고도로 이상화된 시나리오에 기반하기 때문에 보편적인 해법은 존재하지 않는다. 완료 시간을 알 수 없는 작업들을 큐에 배정하는 문제는 잘 알려진 NP-hard 조합 최적화 문제이기 때문이다\cite{ROBSON1980119}. 대부분의 작업 스케줄링 문제는 flow shop, job shop, 단일 기계, open shop, k-기계 병렬 스케줄링의 조합으로 구성되며\cite{b1}, 따라서 대부분 근사 알고리즘으로 다루어진다\cite{b2}.

그럼에도 불구하고 딥러닝 모델 학습·추론 작업을 위한 스케줄러 연구는 계속되고 있다. Ye 등\cite{b3}은 딥러닝 작업 스케줄링의 분류 체계를 제시했다. 첫째는 워크로드 유형(학습, 추론 등)에 따른 분류이고, 둘째는 각 워크로드의 구체적 특성과 목표(효율성, 공정성, 마감 기한 등)에 따른 분류다. 이들은 사용률·공정성·작업 완료 시간에서 뛰어난 Sia\cite{b4}, 다중 상태 프로파일링으로 성능을 최적화하는 Lucid\cite{b5} 등을 최신(state-of-the-art) 스케줄러로 꼽았다. 기존 GPU 스케줄러 대부분은 이러한 방법들의 변형을 채택한다. 따라서 작업 특성이 변하면 스케줄링 정책이나 스케줄러도 그에 맞게 조정되어야 한다.

이처럼 최근 제안된 GPU 스케줄링 접근법들은 내재적 한계를 보인다. 예컨대 learning-to-rank 기반 LLM 스케줄러에서는 Kendall's Tau 순위 지표가 종단 간(end-to-end) 성능을 반영하지 못하는 경우가 관찰되었다. 가장 짧은 70\%의 요청 내부를 무작위로 뒤섞어도 Tau 점수(0.5)는 유지되지만 지연 시간은 크게 증가한다(ShareGPT에서 Llama-8B 기준 1.8배 증가 관찰). 또한 이 방법은 chunk-prefill, prefill-decode 분리(disaggregation) 같은 최신 LLM 서비스 최적화와의 통합이 아직 충분히 검증되지 않았다\cite{fu2024efficient}. 마찬가지로 GPU 공유 워크로드 관리를 위해 설계된 Fragmentation Gradient Descent(FGD)\cite{weng2023beware} 같은 단편화 인지 스케줄링 접근법은 스케줄러 독립성을 보장하기 위해 단편화의 좁은 `한 스텝(one step)' 측정에 집중한다. 이러한 좁은 초점은 클러스터 자원이 풍부하고 단편화가 자연히 낮은 초기 단계에서 차선의(suboptimal) 성능으로 이어질 수 있다.

자원 할당 최적화를 위해 설계된 이들 정교한 스케줄링 접근법은 그 자체의 복잡성과 한계를 동반한다. 예컨대 Optimus\cite{peng2018optimus} 같은 동적 스케줄러는 온라인 자원 성능 모델에 의존해 학습 속도와 수렴을 추정한다. 효과적이긴 하나 Optimus의 성능은 예측 오차에 민감하며, 특히 모델 수렴 추정에서는 최대 20\%의 예측 오차가 관찰되었다. 또한 자원 구성 조정을 체크포인트 기반 방식에 의존하는 점은 오버헤드를 유발하는데, 수백 개의 워커나 파라미터 서버가 관여하는 작업에서는 그 부담이 상당할 수 있다. 별개로, Themis\cite{mahajan2020themis}처럼 공정성과 배치 민감성(placement sensitivity)을 목표로 하는 접근법은 상당한 구조적·계산적 복잡성을 도입하는 방식으로 목표를 달성한다. Themis는 앱이 제출하는 입찰가(bid)를 바탕으로 자원 할당을 결정하는 중앙집중식 라운드 단위 경매 메커니즘을 사용했다. 이 메커니즘은 부분 할당 경매에 솔버(예: Gurobi)를 쓰기 때문에 높은 계산 오버헤드를 유발하며, 경합이 심할 때 95퍼센타일 기준 1,398ms에 달한다. 더욱이 Themis의 효과는 배치 민감 완료 시간 공정성(finish-time fairness) 지표 추정을 위한 애플리케이션의 복잡한 입력에 의존하는데, 이는 비이상적 GPU 배치로 인한 속도 저하를 애플리케이션이 정밀하게 모델링할 것을 요구한다.

이처럼 복잡도가 높고 오버헤드가 따르는 접근법들과 달리, SQUAD는 GPU 클러스터 관리의 핵심 한계를 해소함으로써 기존 스케줄링을 강화하는 상호 보완 메커니즘에 집중한다. SQUAD는 NP-hard 문제를 직접 풀려고 시도하지 않는다. 대신 핵심 스케줄링 정책과 독립적이면서도 호환되는 모든 스케줄러의 성능을 저하시킬 수 있는 주요 병목 --- 자원 단편화에서 비롯되는 HOL 블로킹(Head-of-Line blocking) --- 을 큐 전처리로 해소한다. 이러한 스케줄링 전 단계의 비효율을 해결함으로써 SQUAD는 스케줄러가 최적화된 상태에서 동작하도록 보장한다.

기아는 단지 이론적 우려가 아니다. 실제 GPU 서버 풀에서 수집한 3개월 분량의 작업 로그를 분석하면(\S\ref{algo:intro}의 동기 분석), 백필을 허용하는 스케줄링에서도 큰 작업이 선두에서 막혀 후속 작업이 낮은 할당률 구간에서야 배치되는 HOL 블로킹이 빈번히 관측된다. 이는 정책 계층과 독립적인 전처리 개입의 필요성을 뒷받침한다.

본 연구는 이 문제를 해결하는 SQUAD를 제안하고, 이를 두 시뮬레이터 --- 분포별 합성 트레이스 위의 이산 시간 시뮬레이터와 Philly 111k 작업 규모의 이산 사건 시뮬레이터(실측 오버헤드 보정 포함) --- 로 평가한다. SQUAD의 핵심 기여는 다음과 같다.

\vspace{1em}
\begin{enumerate}
\item \textbf{기아 방지 큐 조정(Starvation-Free Queue Adjustment, SFQA):} 어떤 작업이 서버의 최대 GPU 수를 요청하면, 서버 자원이 부족할 경우 긴 대기 시간을 겪으며 기아 상태에 빠질 수 있다. 이 상태, 즉 HOL 블로킹은 충분한 GPU를 가진 서버가 있어도 후속 작업들을 지연시킨다. SFQA는 대기 중인 작업들의 대기 시간과 자원 요구량을 모니터링하고 큐를 동적으로 재정렬하여, HOL 블로킹을 제거하고 다양한 작업 크기 전반의 공정성을 보장한다. 시뮬레이션에서 SFQA는 기반 스케줄러를 바꾸지 않고도 총 소요 시간을 7.8--14.98\% 단축했다.
\item \textbf{무튜닝 적응형 SFQA(sfqa-auto):} 기존 SFQA는 트레이드오프를 제어하는 계수들을 워크로드별로 사전 탐색해야 한다는 한계가 있었다. 우리는 이 계수 결정을 큐잉 이론에 위임하여 튜닝 노브를 제거한 sfqa-auto를 제안한다. 클러스터 부하 $\rho$로부터 공정성 임계 $\sigma^*=1/(1-\rho)$(Processor-Sharing 공정성 기준)를 직접 유도하므로, 워크로드가 바뀌어도 재튜닝이 필요 없다. 111k 규모 시뮬레이션에서 sfqa-auto는 FIFO 대비 중앙값 대기를 50--61\% 단축하면서 하위 1\% 공정성을 유지하는 유일한 경량 정책이었다 --- 다른 빠른 정책들은 과부하·이종 환경에서 공정성이 0으로 붕괴했다.
\item \textbf{대규모 평가와 직교 베이스라인:} 우리는 SQUAD를 Philly 트레이스 111k 작업 규모(256--1024 GPU, 단일·이종)로 평가하고, 단편화 인지 배치 휴리스틱 FGD를 비롯해 Lucid·Sia 등 최신 기법을 동일 조건에서 비교한다. 이를 통해 SFQA(큐 축)와 배치 최적화(FGD)가 서로 직교하는 축임을 보인다.
\end{enumerate}


본 논문의 나머지 구성은 다음과 같다. \ref{Related}절에서는 관련 연구들을 소개한다. 그다음 절에서는 작업 대기 큐 조정이 왜 필요한지 설명한다. \ref{algo:intro}절에서는 큐 조정의 세부 사항을 수학적으로 제시한다. 이어지는 절에서는 작업 특성이 변하더라도 효과적인 큐 조정이 GPU 서버 풀의 높은 효율을 어떻게 유지하는지 보인다. 아울러 실제 데이터로 보강한 작업 로그에 본 메커니즘을 적용한 성능 결과를 제시한다. 마지막으로 \ref{conclu}절에서 본 연구를 요약하고 향후 발전 방향을 논의한다.

정확한 용어는 `데이터센터 GPU'이지만, 본 논문에서는 간결성을 위해 데이터센터 GPU를 지칭하는 의미로 `GPU'를 사용한다.


\section{Related Works}\label{Related}

\subsection{Efficient LLM Scheduling by Learning to Rank}\label{SCRANK}
전통적인 LLM 서빙 시스템은 출력 생성 길이를 일반적으로 알 수 없기 때문에 FCFS 스케줄링 전략을 사용하며, 이는 심각한 HOL 블로킹을 유발하고 처리량을 떨어뜨린다. Efficient LLM Scheduling by Learning to Rank\cite{fu2024efficient}는 정확한 길이 예측이 불필요하며, 출력 길이의 상대적 순위만 알아도 최적 SJF/SRTF 스케줄링을 근사하기에 충분함을 보였다. 저자들은 소형 보조 모델(예: OPT-125M)로 구현한 learning-to-rank 접근법을 사용하는 새로운 Ranking Scheduler를 제안했다. 이 모델은 순위 품질을 최적화하기 위해 listwise ListMLE 손실로 학습되었고, 순위 품질은 낮은 지연 시간과 직접 상관되는 Kendall 순위 상관계수(Kendall's Tau)로 측정되었다. 이 스케줄러는 반복(iteration) 단위의 즉시 스케줄링을 지원하며, 긴 요청의 공정성을 보장하기 위한 기아 방지(Starvation Prevention) 메커니즘을 포함한다. 최신 서빙 시스템과 통합했을 때 챗봇 서빙에서 p90 지연 최대 2.8배 감소, 합성 데이터 생성에서 처리량 6.5배 증가 등 상당한 성능 향상을 달성했다. 예측 모델의 계산 오버헤드는 무시할 수준으로, 일반적으로 2\% 미만이다. 그러나 저자들은 다음 한계도 지적했다. 높은 Kendall's Tau가 항상 낮은 지연을 보장하지는 않으며(예: 가장 짧은 70\% 요청을 뒤섞어도 Tau$\approx$0.5는 유지되지만 지연은 2배가 됨), request pre-filling 같은 최신 최적화와 함께 평가되지 않았다. 이러한 요인들은 이 방법의 일반성에 잠재적 공백이 있음을 시사한다. 이 방법은 ``출력 길이 추정에 기반한 SJF + 기아 방지''라는 구조에서 SQUAD의 sfqa-auto와 목적을 공유하지만, LLM 서빙의 요청(iteration) 단위에서 동작한다. 반면 SQUAD는 클러스터 수준의 갱(gang) 스케줄링을 대상으로 하며, sfqa-auto는 duration 추정을 선택적으로만 사용하고 추정이 부정확해도 견고하도록 설계된다.

\subsection{FGD}\label{FGC}
사용률 향상을 위해 GPU 공유가 널리 쓰이고 있음에도, 프로덕션 클러스터는 부분(partial) GPU 할당이 유발하는 심각한 GPU 단편화에 시달리며, 이로 인해 수백 장의 GPU가 할당 불가 상태로 남는다(트레이스에서 21--42\%의 단편화율 관찰). 노드 내 GPU들은 서로 교환 가능하지만 GPU 할당은 연속적이어야 하므로, 이 단편화 문제는 고전적 빈 패킹(bin packing)을 넘어서는 정식화를 요구한다. 저자들은 대상 워크로드에서 무작위로 추출한 작업이 주어졌을 때 할당 불가능한 GPU 자원의 기대량을 정량화하는 새로운 통계적 단편화 측도를 도입했다. 이 측도를 바탕으로, 단편화 증가량이 최소가 되는 노드 배정을 선택하여 스케줄링을 안내하는 휴리스틱인 Fragmentation Gradient Descent(FGD)\cite{weng2023beware}를 제안했다. 프로덕션 트레이스(6.2천 장 이상의 GPU)를 사용한 고충실도 시뮬레이션 평가에서 FGD는 기존 패킹 휴리스틱 대비 미할당 GPU를 최대 49\% 줄였으며, 150--290장의 GPU를 추가로 활용 가능하게 했다. FGD는 또한 고립(stranded) 자원을 효과적으로 줄이고 점유가 가장 적은 노드로 작업을 통합한다. FGD는 SQUAD와 같은 문제(단편화)를 공격하지만 서로 직교하는 축에 위치한다. FGD는 \textit{배치 시점}에 단편화 증가가 최소인 노드를 선택하여 단편화 생성을 회피하는 반면, SQUAD의 SFQA는 \textit{큐 순서}에서 단편화의 피해자(기아 작업)에게 우선권을 준다(식 \eqref{eq:01}의 자원 적합도 $R$ 항이 빈 슬롯에 맞는 작업을 우선). 본 논문은 FGD를 원논문 알고리즘 그대로 구현하여 동일 조건의 배치 베이스라인으로 실험에 포함한다(\S\ref{eval:sim}). 실험 결과 FGD는 배치 최적화만으로 중앙값 대기를 크게 줄이지만 큐를 건드리지 않아 과부하에서 기아를 막지 못함을 보여, 효율(배치)과 공정성(큐)이 직교 축임을 실증한다.

\subsection{Optimus}\label{Optimus}
Optimus\cite{peng2018optimus}는 딥러닝(DL) 학습 작업에 고정 자원을 할당하여 자원 효율이 낮고 작업 완료 시간이 길어지는 기존 스케줄러의 한계를 극복하기 위해 제안된, DL 클러스터 맞춤형 작업 스케줄러다. Optimus의 일차 목표는 온라인 자원 성능 모델을 활용해 작업 학습 시간을 최소화하고 자원 효율을 높이는 것이다. Optimus는 온라인 피팅(online fitting)으로 학습 진행을 추적해 모델 수렴(필요 스텝/에폭 수)을 정확히 예측하는 동시에, 동적으로 할당된 자원(워커와 파라미터 서버)의 함수로 학습 속도를 추정한다. 결정적으로, 이 성능 모델링 접근법은 런타임 데이터 위에 구축되어 ML 모델 내부나 클러스터 하드웨어 구성에 대한 사전 지식을 요구하지 않는다.
이러한 예측 성능 모델을 바탕으로 Optimus는 평균 작업 완료 시간(JCT)을 최소화하도록 자원을 동적으로 할당하는 휴리스틱을 구현한다. 또한 최소한의 서버로 학습 속도를 극대화하고 작업을 고르게 배치한다는 원칙에 따라 작업 배치 기법을 설계하여 통신 오버헤드를 완화한다. 아울러 Optimus는 파라미터 서버 아키텍처(예: MXNet) 내부의 심각한 부하 불균형 문제를 식별하고, 모델 슬라이스를 효율적으로 배정하는 Parameter Assignment Algorithm(PAA)을 도입해 이를 해결한다. Kubernetes 환경에서 Optimus는 대표적 클러스터 스케줄러(DRF, Tetris)를 능가하며 JCT를 139\% 개선하는 상당한 성능 향상을 보였다. 그러나 Optimus는 정확한 온라인 성능 모델과 탄력적(elastic) 파라미터 서버 구조를 전제하므로 애플리케이션 수정과 예측 정확도에 의존한다. SQUAD는 이러한 모델이나 앱 협조 없이 대기 큐와 서버 상태만으로 동작하여 정보 요구가 최소라는 점에서 대비된다.

\subsection{Themis}\label{Themis}
Themis\cite{mahajan2020themis}는 장기 공정성 지표로 완료 시간 공정성(finish-time fairness)을 도입한 새로운 스케줄링 프레임워크를 제안했다. 지표 $r$은 공유 클러스터에서의 앱 완료 시간($T_{sh}$)을 전용 1/N 클러스터에서의 이상적 완료 시간($T_{id}$)으로 나눈 비율로 정의되며, 최대 $r$을 최소화하는 것이 목표다. Themis는 중앙 ARBITER가 자원 임대(lease)에 기반한 라운드 단위 경매를 실행하는 2단계 스케줄링 아키텍처로 이를 달성했다. ML 애플리케이션(앱)들은 서로 다른 자원 할당에 대한 추정 $r$ 값을 통해 자신의 성능·배치 선호를 반영한 입찰가를 제출하는 방식으로 참여한다. 이 경매 메커니즘은 바람직한 성질들(전략 무용성(strategy-proofness), 파레토 효율, 무시기성(envy-freeness))을 보장하며, 공유 인센티브(SI) 공정성 기준을 극대화하기 위해 라운드 단위 필터링(Round-by-Round Filtering)을 적용한다. 평가 결과 Themis는 최신 스케줄러들 대비 공정성을 2.25배 이상 개선하고 클러스터 효율을 5--250\% 향상시켰다. Themis의 완료 시간 공정성($T_{sh}/T_{id}$)은 본질적으로 속도 저하(slowdown) 기반 공정성으로, SQUAD의 sfqa-auto가 채택하는 공정성 임계 $\sigma^*=1/(1-\rho)$와 같은 계보에 속한다. 다만 Themis는 솔버 기반 경매(경합 시 95퍼센타일 1,398ms)와 앱이 제출하는 $r$ 값 추정에 의존하는 반면, sfqa-auto는 측정된 부하만으로 임계를 유도하는 $O(n)$ 경량 연산이라는 점이 다르다.

\subsection{Lucid}\label{LU}
Lucid\cite{b5}는 통합 용이성, 확장성, 투명성이라는 세 가지 기본 원칙에 기반한 효율적이고 사용자 친화적인 딥러닝 작업 스케줄링 접근법을 제시했다. 선점 없이 동작함으로써 침습적 수정을 피하고, 사용자의 개입이나 기존 딥러닝 프레임워크의 변경을 요구하지 않는다. 이러한 설계 선택은 운영 경직성, 오류 취약성, 높은 통합·유지보수 오버헤드 같은 문제들을 효과적으로 해소한다. 또한 Lucid는 복잡한 대규모 워크로드를 효율적으로 처리하면서도 시스템 관리자에게 명확하고 조정 가능한 운영 수단을 제공하도록 설계되었다. Lucid의 일차적 초점은 딥러닝 사용자의 요구에 부응해 학습 워크로드의 평균 JCT를 줄이는 것이었다. 자원 효율 개선에 더해 신속한 디버깅 피드백도 제공한다. 향후 Lucid는 공정성 보장, 서비스 수준 협약(SLA) 충족 같은 추가적인 스케줄링 목표를 지원하여 다양한 워크로드 시나리오로 적용 범위를 넓히는 것을 목표로 한다. Lucid는 비침습적 운영을 지향한다는 점에서 SQUAD와 철학을 공유하지만 프로파일링 작업을 요구한다. 또한 우리의 대규모 시뮬레이션에서 Lucid는 작은 작업을 즉시 배치해 중앙값 대기를 극소화하지만 긴 작업의 꼬리 대기를 방치하는 경향을 보였는데, SQUAD는 바로 이 최악 대기(기아)를 유계로 억제하는 데 초점을 둔다.

\subsection{Sia}\label{SI}
Sia\cite{b4}는 이종(heterogeneous) GPU 클러스터에서 자원 적응형(resource-adaptive) 딥러닝 작업을 다루기 위해 특별히 설계된 스케줄러다. GPU 타입·수량·배치 크기 배정을 동적으로 균형 잡는 문제를 다루는데, 이는 방대한 탐색 공간과 작업마다 다른 스케일링 거동 때문에 특히 어렵다. 정수 선형 계획법(ILP) 정식화를 활용하고 탐색 공간을 실용적으로 축소함으로써, Sia는 오버헤드를 최소화하면서 자원을 효율적으로 배정한다. 또한 Sia는 최소한의 초기 프로파일링에 의존하면서 예측을 동적으로 정제하는 혁신적인 처리량 모델링 접근법을 채택하여, 큰 프로파일링 비용 없이 효과적인 자원 할당을 가능하게 한다. 나아가 Sia는 공정성 지표를 유지하고 Megatron 스타일 모델\cite{b5:1} 같은 복잡한 하이브리드 병렬 작업을 효율적으로 확장하는 데 효과적임을 입증했으며, 다양한 클러스터 규모와 워크로드 전반에서 견고한 성능을 유지했다. 이러한 결과는 이종 자원과 작업 가변성이라는 복합적 난제를 다루는 데 있어 Sia의 확장성, 적응성, 우수한 성능을 잘 보여준다. 우리의 시뮬레이션에서도 Sia는 이종 클러스터에서 탁월한 처리량 중심 성능을 보였으나, ILP 평가가 라운드마다 활성 작업 수에 비례해 계산 비용이 급증하여 고부하에서 스케줄링 결정 시간이 SQUAD 계열 대비 200배 이상 커지는 한계를 드러냈다. SQUAD는 작업 내부 적응형 병렬화가 아니라 작업 간 배치를 다루며, 단일 승급 $O(n)$ 연산으로 부하와 무관하게 경량으로 동작한다.

\subsection{Algorithms for Preemptive Co-scheduling of Kernels on GPUs}\label{PCS}
GPU 커널의 선점형 공동 스케줄링 알고리즘\cite{b6}은 최적 makespan을 달성하면서 선점 횟수를 최소화하는 것을 목표로 한다. 이 연구는 최적 선점 makespan이 선형 계획법 해법으로 효율적으로 계산될 수 있음을 보였다. 그러나 최적 makespan을 갖는 모든 해 가운데 최소 선점 횟수를 결정하는 문제는 NP-hard임이 증명되었다. 이러한 발견은 수요가 높은 GPU 환경에서 계산 효율과 최소한의 중단 사이의 균형을 잡는 데 그래프 기반 스케줄링 기법이 효과적임을 보여준다.

\subsection{EASY Backfilling}\label{EASY}
EASY 백필링\cite{mualem2001backfilling}은 HPC 배치 스케줄링에서 수십 년간 검증된 표준 기법으로, 큐 선두 작업을 위해 자원을 예약(reservation)하되 그 예약을 침범하지 않는 후속 작업은 앞당겨 실행(backfill)함으로써 선두 작업의 기아를 막으면서 활용도를 높인다. 그러나 EASY는 각 작업의 실행 시간 추정에 의존하며, 사용자 추정은 흔히 2--10배의 오차를 갖는다. 흥미롭게도 과대추정이 오히려 예약을 보수화하여 성능을 개선하는 ``과대추정 역설''이 보고되었다\cite{tsafrir2007backfilling}. 본 논문은 EASY를 정보 우위(완벽한 duration) 베이스라인으로 사용하여 sfqa-auto의 상한선으로 삼는다. SQUAD의 sfqa-auto는 예약 대신 나이(age) 기반 승급으로 기아를 막아 duration 추정 자체가 불필요하다는 점이 다르다.

\subsection{Kueue}\label{KUEUE}
Kueue\cite{kueue2024}는 쿠버네티스 네이티브 작업 큐잉 시스템으로, 작업을 일시 중단(suspend)·재개하는 방식으로 쿼타(quota) 기반 공정 공유를 제공한다. Kueue는 코어 스케줄러를 교체하지 않고 게이트 계층에서 동작한다는 점에서 SQUAD와 같은 레이어에 속하며, 따라서 본 논문의 가장 직접적인 프로덕션 표준 베이스라인이다. 다만 Kueue의 공정성은 쿼타 분배에 한정되어 HOL 블로킹으로 인한 최악 대기(기아)를 유계로 보장하거나 단편화를 해소하지는 않는다.

\subsection{Baseline selection rationale}\label{baseline-rationale}
베이스라인 비교가 의미를 가지려면 두 시스템이 (i) 같은 결정 변수(같은 스케줄링 객체)를, (ii) 같은 계층에서, (iii) 비교 가능한 정보·인프라 가정 아래 내려야 한다. 이 조건 중 하나라도 어긋나면 그 비교는 스케줄링 로직이 아니라 문제 정의나 인프라를 비교하는 것이 되어 방법론적으로 부당하다. 이 기준에 따라 본 논문은 세 축으로 베이스라인을 선정한다. (1) 같은 계층·같은 정보: FIFO, gate-FIFO 통제군, Kueue --- 순수 정책 효과를 분리한다. (2) 같은 계층·정보 우위: SJF, LAS(Tiresias)\cite{gu2019tiresias}, EASY 백필링(완벽 추정 가정), Themis --- 정보가 유리한 정책에 대해서도 SQUAD가 경쟁함을 보인다. (3) 같은 계층·최신 기법: FGD(배치), Lucid, Sia --- 동일 계층의 가장 강한 학술 스케줄러를 망라한다.

반면 다음 세 시스템은 위 (i)--(iii) 중 하나 이상을 위반하므로 실험 비교에서 제외하며, 이는 누락이 아니라 비교의 타당성을 위한 의도적 선택이다. 각 시스템의 전이 가능한 스케줄링 원리는 이미 위 베이스라인으로 대표된다.
\begin{itemize}
\item \textbf{Learning-to-Rank\cite{fu2024efficient} (계층 불일치):} LLM 서빙 엔진 내부에서 생성 \textit{요청}을 iteration 단위로 재정렬하는 기법으로, 결정 객체(밀리초 단위·매 iteration 자유 선점되는 요청)와 시간 스케일이 클러스터 갱 \textit{작업}(분--일 단위·자유 선점 불가)과 근본적으로 다르다. 학습 작업에는 ``출력 길이''가 없고 iteration 단위 선점도 불가능하여 클러스터 트레이스에 적용하는 것 자체가 정의되지 않는다. 다만 그 본질인 ``예측 순위 기반 SJF + 기아 방지''는 sfqa-auto의 2계층 규칙과 동형이며, 완벽 예측 극한인 SJF가 이미 베이스라인으로 포함되어 그 원리를 대표한다.
\item \textbf{Optimus\cite{peng2018optimus} (가정·정보 불일치):} 핵심 메커니즘인 워커/파라미터 서버의 \textit{탄력적} 재조정은 고정 요청 갱 작업(본 트레이스)에서 조정 대상이 없어 퇴화하며, 기존 배치·정렬 휴리스틱과 구분되지 않는다. 또한 작업별 온라인 수렴 성능 모델과 앱 수준 계측을 요구하는데, 이는 정보 최소 전처리기인 SQUAD와의 직접 비교를 스케줄링 로직이 아닌 인프라 우위 비교로 만든다. 그 목표(duration 인지 평균 JCT 최소화)는 SJF·Themis로, 적응성 축은 이종 탄력 스케줄링 SOTA인 Sia로 실험에 대표된다.
\item \textbf{Preemptive Co-scheduling of Kernels\cite{b6} (계층 자체가 다름):} 단일 GPU 내부에서 연산 \textit{커널}을 시분할 공동 스케줄링하는 기법으로, SQUAD가 할당하는 단위(whole GPU)보다 한 계층 아래에서 동작한다. 공유하는 결정 변수가 없으며, SQUAD가 할당한 GPU 내부에서 함께 동작할 수 있는 상보적 관계다.
\end{itemize}

\section{Motivation}
흥미롭게도, 크게 발전한 딥러닝 기술은 GPU 서버 풀의 비효율 또한 초래했다. Docker Hub\cite{b8}, Hugging Face\cite{b9} 같은 서비스가 가능하게 한 컨테이너\cite{b7} 기반의 재현 가능하고 공유 가능한 학습 환경은 전 세계 딥러닝 연구를 크게 가속했다. 딥러닝을 위한 다양한 ML 프레임워크, 라이브러리, 기본 신경망 아키텍처가 널리 공유되면서 재현성 확보에 들이는 불필요한 시간이 최소화되었다. 그 결과 대부분의 연구자는 일관된 환경에서 알고리즘을 다듬는 데 집중하게 되었고, 모델 아키텍처의 GPU 의존적 구성 요소는 추상화되거나 감추어지는 경우가 많다.

GPU 접근이 추상화되어 있기 때문에 컨테이너는 일반적으로 카드 단위로 GPU와 상호작용한다. 그 결과, 하드웨어가 GPU 할당률을 높이는 타임 슬라이싱을 지원하더라도 모델 학습이나 GPU 서버 풀 관리 과정에서 이 기능을 유연하게 활용할 수 없다. 더욱이 딥러닝 모델의 규모가 커지면서 여러 GPU를 동시에 사용하는 일이 보편화되었다. GPU 서버 풀을 운영하는 동안 완료 시간과 GPU 수가 제각각인 작업들이 끊임없이 제출된다. 그 결과 GPU 서버 풀 내부에 단편화가 발생하여 비효율로 이어진다. 단편화는 다음과 같은 상황에서 발생한다.

\begin{figure}[htb!]
\centerline{\includegraphics[width=\columnwidth]{Pic/Fig_01.png}}
\caption{새로 제출된 작업들이 스케줄링되기 전의 GPU 서버 풀 상태.}
\label{fig:01}
\end{figure}

\begin{figure}[htb!]
\centerline{\includegraphics[width=\columnwidth]{Pic/Fig_02.png}}
\caption{새로 제출된 작업들이 스케줄링되기 전의 작업 대기 큐 상태.}
\label{fig:02}
\end{figure}

그림 \ref{fig:01}과 \ref{fig:02}는 각각 GPU 서버 풀과 작업 대기 큐의 상태를 보여준다. most-allocated 방식을 사용하는 스케줄러는 작업 $\textit{t}_{0}$과 $\textit{t}_{1}$을 할당할 수 있다. 그러나 $\textit{t}_{2}$는 다른 작업들이 완료되어 GPU 슬롯이 비워지기를 기다려야 하므로 스케줄링할 수 없다. 이는 작업 $\textit{t}_{3}$, $\textit{t}_{4}$, $\textit{t}_{5}$가 그 이후에야 스케줄링될 수 있음을 의미한다(그림 \ref{fig:03}).

그림 \ref{fig:03}에 나타난 것처럼, $\textit{t}_{3}$, $\textit{t}_{4}$, $\textit{t}_{5}$를 스케줄링하기에 충분한 GPU 슬롯이 있음에도 이 작업들은 $\textit{t}_{2}$ 때문에 스케줄링되지 못한다. 이것이 HOL 블로킹이다. 이는 앞서 할당된 작업들이 GPU 서버 풀을 단편화시키기 때문이다. GPU 서버 풀의 단편화는 운영 중 지속적으로 발생하며 간헐적으로만 해소된다. 최악의 경우는 모든 GPU 서버에 7장의 GPU가 남아 있는데 HOL 블로킹 작업이 8장을 요청하는 상황이다.

이 경우 GPU 서버 풀은 어떤 작업이 완료되기 전까지 가장 낮은 GPU 할당률에 머무른다. 본 연구는 이 현상을 실험적으로 조사했다. 작업 로그는 most-allocated 스케줄링 방식 기반으로 수집되었으며, 실제 GPU 서버 풀에서 약 3개월에 걸쳐 기록되었다. 우리는 요구 GPU 수를 기준으로 스케줄링 조건을 분석했다. 분석은 Nvidia A30과 A100 GPU로 구성된 GPU 서버 풀, 단일 작업 대기 큐, 대기 중 작업 취소 없음을 가정했다.

\begin{figure}[htb!]
\centerline{\includegraphics[width=\columnwidth]{Pic/Fig_03.png}}
\caption{일부 작업이 스케줄링된 후의 GPU 서버 풀 상태.}
\label{fig:03}
\end{figure}

작업 로그의 특성은 그림 \ref{fig:05}에 나타나 있으며, 각 작업의 요청 GPU 수와 학습 시간을 보여준다. A30과 A100 GPU가 혼재되어 있는 탓에 4장의 GPU를 요청하는 작업이 유난히 많고, 그 외의 GPU 요청 수는 균등 분포를 따른다. 학습 시간은 Weibull minimum 분포를 따른다.
\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_06.png}}
\caption{작업이 할당된 시점의 GPU 서버 풀 할당률}
\label{fig:05}
\end{figure*}

우리는 스케줄링 분석을 통해 HOL 블로킹의 빈도를 조사했다. 특히 GPU 요청 수가 서로 다른 작업 그룹이 스케줄링될 때 GPU 서버 풀의 전체 할당률을 관찰했다. GPU를 1--2장만 요청하는 작업은 서버 풀 할당률이 높은 상태에서도 스케줄링될 수 있다. 그러나 대기 큐 선두에 막힘(blockage)이 있으면 이 작업들은 그 막힘이 해소될 때까지 할당되지 못한다. 그 결과 서버 풀의 전체 할당률이 일정 수준까지 떨어진 뒤에야 할당되는 경우가 잦다. 그림 \ref{fig:06}은 1--8장의 GPU를 요청하는 작업들이 비슷한 풀 할당률에서 할당되고 있음을 보여주며, 이는 막힘의 빈도가 높음을 시사한다.

이 HOL 블로킹은 정책 선택만으로 제거되지 않는다. 여러 스케줄링 정책을 동일 조건에서 비교하면, 중앙값 대기와 최악 대기(기아)는 서로 맞바꾸어지는(trade-off) 관계를 이룬다. 어떤 work-conserving 정책도 두 지표를 동시에 최선으로 만들 수 없으며(총 처리량을 보존하므로 바뀌는 것은 대기의 분포 형태다), 이는 Kleinrock 보존 법칙\cite{kleinrock1976queueing}의 귀결이다. 따라서 본질적 과제는 ``어느 균형점을 택할 것인가''와 ``그 점을 워크로드가 바뀔 때마다 재튜닝 없이 유지할 수 있는가''로 귀결되며, 이것이 SFQA와 그 무튜닝 변형 sfqa-auto의 설계 동기를 형성한다. 본 논문은 이 트레이드오프 구조를 \S\ref{eval:sim}의 대규모 시뮬레이션에서 정량적으로 확인한다.


\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_05.png}}
\caption{실제 작업 로그의 특성.}
\label{fig:06}
\end{figure*}

완료 시간과 스케줄링 요청 시점을 알 수 없는 무한한 작업들을 최적으로 스케줄링하는 문제는 NP-hard다\cite{ROBSON1980119}. 따라서 이 문제는 스케줄링 알고리즘만으로는 완전히 해결될 수 없다. 이에 본 연구는 스케줄링 알고리즘의 개선보다, 단편화에서 비롯되는 HOL 블로킹의 해소를 우선시한다. 그 결과 우리는 작업 대기 큐를 재정렬하는 큐 조정(queue adjustment) 메커니즘을 제시한다.

우리는 HOL 블로킹 해소 메커니즘의 분석과 신속한 반복 개발을 위해 작업 스케줄링용 자체(in-house) 시뮬레이션을 개발했다. 이 시뮬레이터는 작업 로그를 파싱하고 다양한 방식으로 스케줄링에 응답한다. 기존 메커니즘에 영향을 주지 않고 새로운 스케줄링 방법론을 추가할 수 있다. 이는 큐 조정이 작업 스케줄링 방식과 독립적으로 동작함을 의미한다.

\section{Resolving HOL blocking in Job Queue and GPU Fragmentation}\label{algo:intro}
우리는 작업 대기 큐의 HOL 블로킹을 해소하기 위한 기아 방지(starvation-free) 알고리즘(SFQA)과, 계수 튜닝을 제거한 그 적응형 변형(sfqa-auto)을 제안한다. SFQA는 코어 스케줄러 실행 전에 동작하는 전처리기로서 작업 대기 큐를 재정렬하여, 단편화에서 비롯되는 HOL 블로킹을 직접 완화한다. SFQA는 코어 스케줄러의 종류·메커니즘과 무관하게 작동하며, 그 트레이드오프는 나이 가중치 계수 $\alpha$로 제어된다. sfqa-auto는 이 $\alpha$마저 부하에서 유도하여 제거한다.
\subsection{Starvation-free Algorithm}
본 연구가 제안하는 기아 방지 큐 조정은 사용하는 스케줄러의 종류나 방식과 무관하게 GPU 서버 풀의 높은 할당률을 보장하는 것을 목표로 한다. 기아 방지 알고리즘은 스케줄러 실행 전에 동작하여 작업 대기 큐를 최적화함으로써, 스케줄러의 종류나 메커니즘과 무관하게 최적의 스케줄링 성능을 가능하게 한다.

HOL 블로킹은 GPU 서버 풀 운영 중 빈번히 발생하며 결국에는 해소되지만, 그 해소에 오랜 시간이 걸릴 수 있다. 이 지연은 전체 할당률에 부정적인 영향을 미친다. HOL 블로킹을 예방하는 스케줄링은 NP-hard 문제이며, HOL 블로킹이 언제 발생할지 예측하는 것은 거의 불가능하다. 현실 세계에서 GPU를 요청하는 작업의 유형과 요구 자원이 예측 불가능하기 때문이다. 따라서 HOL 블로킹을 예방하려는 스케줄링은 간접적인 접근에 머무른다. 기아 방지 알고리즘은 대기 큐에 있는 작업들의 우선순위를 직접 수정함으로써 막힘 문제를 해결한다. 우리는 이러한 수정이 낳는 트레이드오프 또한 특정 계수들을 사용해 고려한다. 큐를 조정하면 막힘에 가로막힌 작업들이 스케줄링될 수 있게 되지만, 최악의 경우 막힘에 해당하는 작업이 영원히 할당되지 못할 수 있다는 트레이드오프가 존재한다. 본 알고리즘은 최상위 우선순위 작업의 기아를 방지하도록 설계되었다. HOL 블로킹을 임의로 제거하거나 우선순위를 임의로 조정하면 공정성 문제가 발생할 수 있다.

$P^*$를 작업 $t_0^*$, $t_1^*$, $t_2^*$, \dots, $t_n^*$의 조정된 우선순위 값이라 하자. $P^*$는 스케줄링이 시작되기 전에 계산되어 재부여된다. 스케줄러는 수정된 작업 우선순위를 사용한다. 조정된 작업 우선순위 $P^*$는 다음과 같이 정의된다:

\begin{equation}
P^* = P + \alpha A \times R
\label{eq:01}
\vspace{1em}
\end{equation}

첫 번째 항 $P$는 작업 $t_0$, $t_1$, \dots, $t_n$의 원래 우선순위를 나타내며, 여기서 $n$은 서버 수의 3배다. 구체적으로 우선순위는 $p_0$ = 1, $p_1$ = 0.5, $p_2$ = 0.25와 같이 정의되어 이후 우선순위가 절반씩 감소하며, 값이 클수록 우선순위가 높다. 두 번째 항은 기아 지수(starvation index)를 나타내는 변수 $\alpha A \times R$로 구성된다. 여기서 $\alpha$는 기아 지수가 중요성을 얻는 속도를 결정하는 계수다. 예컨대 대기 큐의 두 번째 작업의 우선순위가 6시간 안에 역전되도록 하려면, $R$을 1로 가정할 때 $\alpha$는 최소 0.013889로 설정되어야 한다. 수치로 보면 $P_1$이 0.5일 때 $0.013889 \times 36 \times 1 = 0.500004$가 계산되어 갱신된 우선순위는 1.000004가 되고, 이는 $P_0$ 값 1을 넘어서므로 우선순위가 역전된다. 이때 $A$는 작업의 나이(age)로 10분마다 1씩 증가하며, $R$은 자원 적합도 지수(resource suitability index)다. $A$는 작업이 스케줄링되면 초기화된다. $R$ 값은 요구 가속기 수가 가용 수와 정확히 일치하면 1이고, 가속기가 하나 추가될 때마다 $0.1$씩 감소한다. Algorithm \ref{alg:adjust_wait_queue}는 \eqref{eq:01}을 사용한 기아 방지 알고리즘의 구현을 보여준다.

\begin{algorithm*}
\caption{Starvation-free Algorithm}\label{alg:adjust_wait_queue}
\textbf{Input:} \\
\hspace*{4mm} Server count : \( \boldsymbol{n} \), Wait queue length : \( \boldsymbol{l_{wq}} \) \\
\hspace*{4mm} Servers :\( \boldsymbol{S_i} \in \{S_0, S_1, \ldots, S_{n-1}\} \), \\ 
\hspace*{4mm} Accelerator count in $S_i$: \( \boldsymbol{M_i} \in \{M_0, M_1, \ldots, M_{n-1}\} \), \\ 
\hspace*{4mm} Resource suitability index : \( \boldsymbol{R_i} = 0, \forall i \in \{1, 2, \ldots, 8\} \), \\
\hspace*{4mm} Wait Queue : \( \boldsymbol{WQ_i} \in \{WQ_0, WQ_1, \ldots, WQ_{l-1}\} \), \\
\hspace*{4mm} Task in $WQ$ : \( \boldsymbol{t_i} \in \{t_0, t_1, \ldots, t_{l-1}\} \), \\
\hspace*{4mm} Accelerator request of $t_i$ : \( \boldsymbol{a_{t_i}} \) \\
\hspace*{4mm} Wait Age : \( \boldsymbol{A_i} \in \{A_0, A_1, \ldots, A_{m-1}\} \)
\begin{flushleft}
\textbf{Procedure:} Updated wait queues
\end{flushleft}
\begin{algorithmic}[1]
\STATE $R \gets 0$
\STATE {}
\FOR{ $i \gets 0$ to $n-1$}
    \FOR{$j \gets 1$ to $M_i$}
        \IF{$a_{S_i} > j$}
            \STATE $R_j \gets \max\{R_j, 1 - ( {a_{S_i}} - j ) \times 0.1\}$
        \ENDIF
    \ENDFOR
\ENDFOR
\STATE {}
\FOR{ $i \gets 0$ to $l_{wq}-1$}
    \STATE $P_i \gets 1/2^i$
    \STATE $P_i^* \gets P_i + \alpha A_i \times R_{a_{t_i}}$
\ENDFOR
\STATE {}
\STATE $(P_{i_{\max}}^*, i_{\max}) \gets \max(P^*)$
\STATE $P_0 \gets P_{i_{\max}}^*$
\STATE {}
\FOR{$j \gets 1$ to $i_{\max}$}
    \STATE $P_j \gets P_{j-1}^*$
\ENDFOR
\end{algorithmic}
\end{algorithm*}

\subsection{Zero-knob Adaptive SFQA (sfqa-auto)}\label{sfqa-auto}
식 \eqref{eq:01}의 SFQA는 효과적이지만, 나이 가중치 계수 $\alpha$를 워크로드별로 사전에 탐색해야 한다. 우리는 이 계수 결정을 큐잉 이론에 위임하여 튜닝 노브를 제거한 변형 sfqa-auto를 제안한다. 핵심 착상은 스케줄러를 다음 제약 최적화 문제의 온라인 근사로 보는 것이다:
\begin{equation}
\min~ \text{mean JCT} \quad \text{s.t.}~ \mathrm{BSLD}_i(t) \le \sigma^*(t)~~\forall i,
\tag{1a}
\label{eq:auto-opt}
\end{equation}
여기서 $\mathrm{BSLD}_i=(W_i+s_i)/\max(s_i,\tau)$는 유계 속도 저하(bounded slowdown)\cite{feitelson1998metrics}이고, $W_i$는 대기 시간, $s_i$는 서비스 시간, $\tau$(=10s)는 초단기 작업의 퇴화를 막는 문헌 표준 하한이다\cite{mualem2001backfilling}. 공정성 임계 $\sigma^*(t)$는 측정된 클러스터 부하 $\rho(t)$로부터 직접 유도된다:
\begin{equation}
\sigma^*(t) = \frac{1}{1-\rho(t)}.
\tag{1b}
\label{eq:sigma}
\end{equation}
이 값은 Processor-Sharing(PS)이 달성하는 평균 속도 저하이자 Wierman--Harchol-Balter의 공정성 기준\cite{wierman2003classifying}으로, ``현재 부하에서 이상적 공평 분배(PS)였다면 겪었을 속도 저하까지는 정상''이라는 의미를 갖는다. 따라서 $\sigma^*$는 임의 상수가 아니라 큐잉 이론의 공정성 정의 그 자체다. $\rho$는 점유 GPU 비율의 지수 가중 이동 평균으로 추정하며, 모든 양은 측정값에서 계산되어 튜닝 대상 노브가 없다.

각 대기 작업의 공정성 예산은 \eqref{eq:auto-opt}의 제약을 $W_i$에 대해 풀어 얻는다:
\begin{equation}
W^*_i(t) = \sigma^*(t)\cdot\max(s_i,\tau) - s_i, \qquad u_i(t) = \frac{A_i}{W^*_i(t)},
\tag{1c}
\label{eq:urgency}
\end{equation}
여기서 $A_i$는 작업의 나이이고 $u_i$는 무차원 긴급도(공정성 예산 소진율)다. $u_i \ge 1$은 작업 $i$의 BSLD가 PS-공정 한계 $\sigma^*$를 초과함, 즉 공정성 제약 위반을 뜻한다. 스케줄링 규칙은 제약 최적화의 표준 구조를 따르는 2계층 순서다:
\begin{itemize}
\item \textbf{계층 1}(위반, $u_i\ge 1$): $u_i\cdot R_i$ 내림차순 --- 공정성 복구 우선,
\item \textbf{계층 2}(충족, $u_i<1$): $s_i$ 오름차순(SJF) --- 평균 완료 시간 최적\cite{smith1956various},
\end{itemize}
계층 1 전체가 계층 2보다 앞선다. $R_i$는 식 \eqref{eq:01}의 자원 적합도 지수를 계승하여 같은 위반 수준이면 자원이 맞는 작업을 우선한다. 제약이 느슨하면 목적 함수의 최적해(SJF)로, 제약이 활성화되면 위반 해소로 자연스럽게 전환되므로 별도의 댐핑 휴리스틱이 필요 없다. $u_i$는 대기 중 단조 증가하므로 계층 승격은 비가역적이고 진동이 없다. 부하가 변하면 $\sigma^*(t)$가 따라 변하므로, 고정 계수 $\alpha$가 워크로드 시간 스케일 변화에서 균형점을 잃는 문제(원본 SFQA의 한계)가 원천적으로 제거된다. 즉 sfqa-auto는 SFQA의 ``나이로 기아를 방지한다''는 정체성을 유지하면서 계수 결정을 큐잉 이론에 위임한 것이며, 원본의 고정 노브 $\alpha$는 $\sigma^*(t)$(측정값)로 흡수된다.

SFQA(및 그 무튜닝 변형 sfqa-auto)는 스케줄러와 독립적으로 동작하며, 일정 주기로 동작하는 스케줄러보다 먼저 실행된다. 이는 동작 결정에 작업 대기 큐와 서버 풀 내 현재 작업 할당 상태만을 사용하므로, 코어 스케줄러의 종류나 메커니즘과 무관하게 작업 대기 큐를 최적화하는 전처리기로 기능한다. 다만 본 연구는 작업 대기 큐를 직접 관리하므로, 큐에 대한 스케줄링을 자체적으로 수행하거나 누적 지표·컨텍스트에 의존하는 스케줄러는 대상으로 하지 않는다.

\section{Optimization of Coefficient $\alpha$}
기아 방지 알고리즘은 트레이드오프 제어를 위해 나이 가중치 계수 $\alpha$를 사용한다. 이 절에서는 $\alpha$가 성능에 미치는 영향과 그 최적값이 워크로드에 따라 어떻게 달라지는지를 분석한다. 이 분석의 결론 --- 최적 $\alpha$가 워크로드 특성에 따라 크게 달라진다는 사실 --- 은 고정 계수 방식의 근본적 한계를 드러내며, 이는 곧 \ref{sfqa-auto}절의 무튜닝 sfqa-auto의 설계 동기가 된다.

\subsection{Trade-off analysis and definition of related metrics}
$\alpha$는 HOL 블로킹 제거에 도움을 주지만, 값이 지나치게 크면 막혀 있던 작업이 다른 작업을 과도하게 추월하여 새로운 불공정을 낳을 수 있다. 적절한 $\alpha$를 정하려면 이 트레이드오프를 정량화해야 한다. 이에 각 작업의 누적 대기 시간을 요청 가속기 수로 묶어 트레이드오프로 산정하며, 이를 누적 평균 거리(accumulated average distance, AAD)로 정의한다.
\begin{equation}
AAD = \sum_{k=1}^8 \frac{(WT'_k - WT_k)}{2^{8-k}}
\tag{2}
\label{eq:07}
\end{equation}

식 \eqref{eq:07}은 $\alpha$ 적용 전후 대기 시간의 가중 평균 차이를 측정한다. 요청 하드웨어 가속기 수로 묶인 작업들의 원래 대기 시간 합을 $WT_1$, \dots, $WT_8$로, $\alpha$ 적용 후의 갱신된 대기 시간을 $WT'_1$, \dots, $WT'_8$로 표기한다. 요청 가속기 수가 하나 줄어들 때마다 거리의 가중치는 절반이 된다. 한편 대기 시간 차이들 가운데 최댓값을 누적한 누적 최대 거리(accumulated maximum distance, AMD)는 식 \eqref{eq:08}로 정의된다. 여기서 $w_t$는 요청 가속기 수 기준 작업 대기 시간 집합, $w'_t$는 그 최대 대기 시간 집합이다.

\begin{equation}
AMD = \sum_{k=1}^8 \max \left( \frac{w'_t - w_t}{2^{8-k}} \right)
\tag{3}
\label{eq:08}
\vspace{1em}
\end{equation}

이 지표들의 계산은 작업 대기 시간의 평균 편차와 최대 편차를 모두 고려하여, 스케줄링의 공정성을 강제하고 기아나 과도한 대기로 인한 성능 저하를 방지한다.

\subsection{Objective function}
$\alpha$는 wall time, AAD, AMD를 최소화하도록 조정된다. 최적값을 결정하기 위해 $\alpha$ 값 범위(0.01--1.99, 0.01 간격)에 대해 탐색을 수행하고, 각 값에서 세 지표를 계산한다. 지표 간 공정한 비교를 위해 세 지표 모두 min--max 스케일러로 정규화한 뒤 합산하여 목적 함수를 정의하며, 이를 최소화하는 $\alpha$를 선택한다.

\begin{equation}
Obj_i^{sfa} = walltime_i^{norm} + AAD_i^{norm} + AMD_i^{norm}
\tag{4}
\label{eq:09}
\vspace{1em}
\end{equation}

\subsection{From fixed coefficients to zero-knob adaptation}
고정 $\alpha$ 방식의 근본 한계는 \textit{최적 $\alpha$에 단일 값이 없다}는 것이며, 이것이 무튜닝 sfqa-auto를 설계한 직접적 동기다. 앞 방식으로 8가지 작업 분포에 대해 최적 $\alpha$를 탐색하면(표 \ref{tab:02}, 그림 \ref{fig:motivation}), 최적 $\alpha$가 분포에 따라 0.22에서 1.63까지 약 7배 범위로 달라진다. 따라서 한 번 고정한 $\alpha$는 임의의 워크로드에 대한 타협일 뿐이며, 워크로드가 바뀔 때마다 다시 탐색해야 한다. 이 한계는 ``고정된 절대 시간 단위''에서 비롯된다 --- 나이 가중치가 절대 초 단위로 고정되어 있어, 작업 분포나 대기 시간 스케일이 달라지면 의도한 균형점을 벗어난다.

sfqa-auto는 이를 \eqref{eq:sigma}의 $\sigma^*(t)=1/(1-\rho)$로 대체하여 모든 양을 측정된 부하에서 무차원으로 유도하므로, 워크로드가 바뀌어도 재튜닝이 필요 없다(튜닝 비용 0). 이 무튜닝 적응의 대가는 작은 중앙값 양보이며, 그 대신 부하·이질성 전반에서 일관되게 높은 공정성을 얻는다는 점을 \S\ref{eval:sim}에서 정량적으로 보인다.

\begin{figure}[htb!]
\centerline{\includegraphics[width=\columnwidth]{Pic/Fig_18.pdf}}
\caption{무튜닝 sfqa-auto의 설계 동기. 최적 고정 $\alpha$가 8가지 작업 분포에 따라 약 7배(0.22--1.63) 달라져 단일 값이 존재하지 않는다. 따라서 워크로드마다 재탐색이 필요하며, sfqa-auto는 $\alpha$를 측정된 부하에서 유도해 이 탐색 자체를 제거한다.}
\label{fig:motivation}
\end{figure}

계수 $\alpha$를 제거한 뒤 sfqa-auto에 남는 소수의 내부 상수가 또 다른 숨은 노브가 아님을 보이기 위해, 별도의 1차원 민감도 분석을 수행했다(그림 \ref{fig:sensitivity}). C++ 시뮬레이터로 12개 구성을 스윕한 결과, 자원 적합도 페널티 $R$(0.05--0.3)과 우선순위 밑 $P$(1.5--4)는 makespan 변동이 $\pm$2\% 이내로 둔감했고, 큐 재정렬 창 $m$만 $m$=1에서 +8.9\%로 민감했으며 $m\ge 2$에서는 1.4\% 이내였다(본 연구는 안전값 $m$=3 채택). 즉 이 상수들은 임의로 선택된 값이 아니라 넓은 범위에서 안정적이며, 유일하게 의미 있는 설계 선택($m\ge 2$)도 명확한 근거를 갖는다.

\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_22.pdf}}
\caption{잔여 내부 상수의 민감도(C++ 시뮬, makespan 변화율). 자원 적합도 페널티 $R$과 우선순위 밑 $P$는 넓은 범위에서 $\pm$2\% 이내로 둔감하고, 큐 재정렬 창은 $m$=1에서만 민감하다($m\ge 2$는 안전). 임의 상수가 아님을 보인다.}
\label{fig:sensitivity}
\end{figure*}

\section{Experimental Evaluation}
평가는 하나의 질문 --- \textit{어떤 정책이 효율과 공정성의 균형점을 차지하며, 그 점을 워크로드·부하가 바뀌어도 무튜닝으로 유지하는가} --- 을 두 단계로 답하도록 구성된다. (i) 분포별 합성 트레이스 위의 이산 시간 C++ 시뮬레이터로 SFQA의 스케줄러 비종속성과 계수 효과를 확인하고(\S6.1--6.2), (ii) Philly 111k 작업 규모의 이산 사건 시뮬레이터로 분포·규모·이질성 전반(256--1024 GPU, 단일·이종)으로 일반화한다(\S\ref{eval:sim}). 각 그림은 이 사슬의 한 주장을 뒷받침한다.

SFQA는 단순하고 유연하며 강력한 메커니즘이다. 우리가 아는 한, 작업 대기 큐의 HOL 블로킹을 직접 다루는 기존 방법은 없다. SFQA의 유연성과 성능을 검증하기 위해, 다양한 작업 분포에 대해 최적 $\alpha$를 식별하고 그 값을 사용했을 때의 스케줄링 성능 개선을 평가했다. 다양한 작업 분포의 최적 $\alpha$를 식별하기 위해 정규, 지수, 로그 정규, 감마, 베타, Weibull minimum, 균등, 푸아송의 8개 분포로 가속기 요청과 작업 실행 시간을 시뮬레이션했다. 생성된 모든 작업 로그는 1,000개의 작업을 포함한다. $\alpha$ 탐색 범위는 0.01--1.99(0.01 간격)이다.
\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_10.png}}
\caption{증강 작업 로그의 특성.}
\label{fig:10}
\end{figure*}

성능 개선을 측정하기 위해 두 종류의 작업 로그를 사용했다. III절의 실제 로그와 그것을 기반으로 한 증강(augmented) 작업 로그로, 각각 368개와 3,001개의 작업 항목을 포함한다. 평가는 두 작업 로그를 사용하여 SFQA 적용 전후의 결과를 비교했다. 사용된 $\alpha$는 위의 탐색 공간과 동일하다. 증강 작업 로그의 특성은 그림 \ref{fig:10}에 제시되어 있으며, 각 작업의 요청 GPU 수와 wall time을 포함한다.

최적 계수의 결정과 메커니즘 간 성능 비교에는 시뮬레이터를 사용했다. 시뮬레이션은 Intel Core Ultra 7 155H 3.80GHz 프로세서와 32GB RAM을 갖춘 Windows 11 시스템에서 실행되었으며, 시뮬레이터는 Visual Studio Community 2022로 C++로 개발되었다.

\subsection{Optimal value of coefficient $\alpha$}
\begin{figure*}[htb!]
%\begin{figure*}[H]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_11.png}}
\caption{8가지 작업 분포의 특성.}
\label{fig:11}
\end{figure*}
8가지 분포 작업 로그에 대한 시뮬레이션 결과는 각 계수의 최적값을 나타낸다. 그림 \ref{fig:11}은 각 작업 로그의 특성을 보여준다. 표 \ref{tab:02}는 분포별 최적 $\alpha$를 보여준다. 최적 $\alpha$가 작업 로그의 특성에 따라 0.22에서 1.63까지 달라지는데, 이는 고정된 $\alpha$를 쓰기보다 워크로드 특성에 맞춰 조정하는 것이 유리함을 시사한다.

이 결과는 SFQA가 다양한 유형의 워크로드 전반에서 효과적으로 동작함을 입증한다. 나아가 각 워크로드의 특성에 맞춘 $\alpha$ 조정의 중요성을 부각하며, 이것이 무튜닝 sfqa-auto의 동기가 된다.

\begin{table}[htb!]
\centering
\caption{계수의 최적값}
\begin{tabular}{@{}lc@{}}
\toprule
\textbf{Distribution methodology} & \textbf{$\alpha$} \\ \toprule
Normal & 0.53 \\ 
Exponential & 0.44 \\ 
Log-normal & 1.63 \\
Gamma & 0.22 \\
Beta & 0.26 \\
Weibull minimum & 0.55 \\
Uniform & 0.5 \\
Poisson & 0.22 \\ \bottomrule
\end{tabular}
\label{tab:02}
\end{table}

\subsection{Evaluation of scheduling improvement}
앞선 계수 최적 탐색 방법으로 실제 작업 로그와 증강 작업 로그 각각의 계수를 정할 수 있었다. 계수는 표 \ref{tab:03}에 정리되어 있으며, 그림 \ref{fig:12}(A)와 \ref{fig:12}(B)는 두 작업 로그로 시뮬레이션했을 때 최적 계수가 wall time, 트레이드오프 지표, 할당률, 사용률에 미치는 효과를 보여준다. X축은 시간을 나타내며 한 단위는 10분에 해당한다. GPU 서버 풀 할당률과 총 사용률도 함께 얻었다. 그림 \ref{fig:12}(C)와 \ref{fig:12}(D)에서 실제 작업 로그의 할당률과 사용률 모두 오른쪽으로 이동하는데, 이는 더 높은 할당률과 사용률을 나타낸다.

\begin{table}[htb!]
\centering
\caption{작업 로그별 최적 계수 값}
\begin{tabular}{@{}ccc@{}}
\toprule
\textbf{Coefficient} & \textbf{Actual job log} & \textbf{Augmented job log} \\ \toprule
$\alpha$ & 0.03 & 0.72 \\ \bottomrule
\end{tabular}
\label{tab:03}
\end{table}

\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_12.png}}
\caption{두 작업 로그에 대한 시뮬레이션 결과 비교.}
\label{fig:12}
\end{figure*}

표 \ref{tab:04}는 알고리즘 조합별 wall time을 정리한 것이다. 실제 작업 로그와 증강 작업 로그의 시뮬레이션 결과는 서로 다른 거동을 보였다. SFQA 적용 전 실제 작업 로그와 증강 작업 로그의 wall time은 각각 165일 2시간 1분, 1,547일 6시간 1분이었다. 알고리즘 적용 후 두 작업 로그의 wall time은 각각 약 13일과 80일 단축되었다. 실제 작업 로그는 wall time에서 7.8\%의 성능 향상을 달성했고, 증강 작업 로그에서는 14.98\%의 성능 향상이 달성되었다. 결과는 그림 \ref{fig:12}(A), \ref{fig:12}(B), 표 \ref{tab:04}에 제시되어 있다. 증강 작업 로그는 실제 작업 로그를 기반으로 생성되었다. 증강 작업 로그의 첫 부분은 실제 작업 로그와 동일한 368개의 작업으로 구성되고, 이후 실제 작업 로그의 거동을 반영하도록 증강이 수행되었다. 이 증강의 결과로 4 GPU를 요구하는 작업 그룹의 지배력이 줄어들었다. 그 결과 증강 작업 로그는 실제 작업 로그보다 약 8.1배 커졌다. 로그가 많다는 것은 워크로드가 시간에 걸쳐 누적될수록 GPU 서버 풀의 단편화가 심해짐을 의미한다.

\begin{table}[htb!]
\centering
\caption{시뮬레이션 결과의 Wall Time}
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{Applied Algorithm} & \textbf{Actual job log} & \textbf{Augmented job log} \\ \toprule
기본(미적용) & 165d 02h 01m & 1,547d 06h 01m \\
SFQA 적용 & 151d 01h 38m & 1,464d 06h 22m \\ \bottomrule
\end{tabular}
\label{tab:04}
\end{table}

표 \ref{tab:04}는 SFQA 적용 전후의 wall time을 보여준다. SFQA는 실제 작업 로그와 증강 작업 로그 모두에서 전체 작업 처리 시간을 일관되게 단축한다.

\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_13.png}}
\caption{알고리즘 조합별 할당률 비교.}
\label{fig:13}
\end{figure*}

할당률 관점에서도, 실제 작업 로그에 SFQA를 적용하면 미적용보다 높은 할당률을 달성한다. 이는 그림 \ref{fig:13}(A)에 나타나 있으며, 증강 작업 로그(그림 \ref{fig:13}(B))에서도 동일하다. 그림 \ref{fig:13}(A)에서 SFQA 적용(Optimized로 표기)은 기본 대비 할당률 분포가 오른쪽으로 크게 이동하여 더 높은 할당률을 보인다. 이 효과는 그림 \ref{fig:13}(B)의 증강 작업 로그 시뮬레이션 결과에서 더욱 뚜렷하다.

%\begin{figure*}[htb!]
\begin{figure*}[H]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_14.png}}
\caption{다양한 스케줄링 방법론의 할당률 비교.}
\label{fig:14}
\end{figure*}

SFQA는 작업 대기 큐를 직접 다루므로 스케줄러 방법론에 구애받지 않는(agnostic) 메커니즘이다. 앞선 평가에서는 most-allocated 스케줄러를 사용했다. 표 \ref{tab:05}는 다른 세 스케줄러에 대해 SFQA가 달성한 성능 향상을 정리한 것이며, 증강 작업 로그와 동일한 $\alpha$를 사용했다.

\begin{table}[htb!]
\centering
\caption{다른 스케줄러 방법론 적용 결과}
\begin{tabular}{@{}p{1cm}ccc@{}}
\toprule
 & \textbf{Compact} & \textbf{Round robin} & \textbf{MCTS} \\ \toprule
기본(미적용) & 1,700d 11h 01m & 1,588d 18h 01m & 1,605d 08h 01m\\
최적 계수 적용 & 1,462d 20h 30m & 1,467d 08h 27m & 1,462d 11h 31m \\
이득$^{\mathrm{a}}$ & 238d (14\%) & 121d (7.6\%) & 143d (8.9\%) \\ \bottomrule
&&&$^{\mathrm{a}}$일수만 집계
\end{tabular}
\label{tab:05}
\end{table}
Compact와 라운드 로빈은 단순한 스케줄링 방법론이다. MCTS(Monte Carlo Tree Search)\cite{b11}는 무작위성에 기반한 탐색 방법이다. 최근 Hofmann C\cite{b12}, Kung H\cite{b13} 등 자원 스케줄링에 MCTS를 도입한 연구들이 등장했다. 본 연구의 MCTS 시뮬레이션에서는 most-allocated 방식을 함께 사용했다. 각 스케줄링 방법론의 할당률 비교는 그림 \ref{fig:14}에 제시되어 있다. 그림에서 보듯, 최적화된 계수는 Compact, 라운드 로빈, MCTS 세 스케줄링 방법론 모두에서 할당률 분포를 일관되게 더 높은 활용 쪽으로 이동시키며, 이는 SFQA의 스케줄러 비종속적(scheduler-agnostic) 성질을 입증한다. 표 \ref{tab:05}는 이 개선을 정량적으로 확인해 준다. wall time은 Compact, 라운드 로빈, MCTS 스케줄러에서 각각 약 14\%, 7.6\%, 8.9\% 단축되었다. 이 결과는 SFQA가 GPU 할당 효율을 개선할 뿐 아니라, 기반 스케줄링 전략과 무관하게 안정적인 성능 이득을 제공함을 보여준다.

\subsection{Scale generalization via discrete-event simulation}\label{eval:sim}
\S6.1--6.2의 C++ 시뮬레이터는 분포별 합성 트레이스에 한정되므로, 우리는 Philly 트레이스 전체(약 111k 작업)를 256, 512, 1024 GPU 클러스터(단일·이종 구성)에서 평가하는 이산 사건 시뮬레이터를 개발했다. 시뮬레이터에는 \S\ref{eval:overhead}의 작업당 고정 오버헤드를 주입했다. 비교에는 FIFO·SJF·LAS·Kueue·EASY·Themis·SFQA·sfqa-auto에 더해, 단편화 인지 배치 휴리스틱 FGD와 학술 최신 기법 Lucid·Sia를 충실히 구현해 포함했다.

그림 \ref{fig:scale}와 표 \ref{tab:sim}이 결과를 요약한다. sfqa-auto는 FIFO 대비 중앙값 대기를 50--61\% 단축하면서 하위 1\% 순서 공정성(p1)을 50--76 수준으로 유지하여, ``빠르면서 공정한''을 동시에 달성하는 유일한 경량 정책이었다. 대조적으로 SJF·Themis·LAS는 중앙값 대기는 최소였으나 이종 클러스터에서 하위 1\% 공정성이 0으로 붕괴했다(완전 기아). Lucid는 작은 작업을 즉시 배치해 중앙값이 극히 작았으나 꼬리 대기가 컸다. Sia는 이종 클러스터에서 처리량 중심 지표가 탁월했으나, ILP 평가가 부하에 비례해 폭증하여 256 GPU 고부하에서 스케줄링 계산이 약 2시간 --- 경량 정책의 200배 이상 --- 에 달했다. 부하·이질성이 심할수록 sfqa-auto의 상대적 이득이 커진다.

백분위 요약을 넘어 큐잉 지연의 전체 분포를 보면(그림 \ref{fig:cdf}) 정책 간 차이가 더 분명하다. FIFO는 분포 전체가 오른쪽(느림)에 치우치고, SJF·Lucid는 몸통을 왼쪽으로 극단적으로 당기지만 상위 분위에서 급격히 꺾여 긴 꼬리(기아)를 남긴다. sfqa-auto는 분포의 몸통을 왼쪽으로 당기면서도 꼬리를 함께 억제하여, 빠름과 공정의 균형이 분포 형태로 드러난다. 부하 의존성도 전 정책·전 클러스터 규모에 걸쳐 일관된다(그림 \ref{fig:loadcurve}). 클러스터가 작아져 부하가 깊어질수록 중앙값(q\_p50)과 최악 대기(q\_max)의 정책 간 격차가 벌어지며, 공정성(p1)에서는 SJF·Themis·LAS·FGD가 고부하 이종에서 0으로 붕괴하는 반면 sfqa-auto만 FIFO에 버금가는 수준을 유지한다.

\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_19.pdf}}
\caption{큐잉 지연의 전체 분포(CDF, Philly 111k, 512 GPU 단일·이종). sfqa-auto(굵은 빨강)는 분포 몸통을 왼쪽으로 당기면서 꼬리도 억제한다. SJF·Lucid는 몸통은 빠르나 꼬리가 길고, FIFO는 분포 전체가 느리다.}
\label{fig:cdf}
\end{figure*}

\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_20.pdf}}
\caption{이종 클러스터에서의 부하 의존성(Philly 111k, 전 정책). 좌: 중앙값 q\_p50, 중: 최악 대기 q\_max, 우: 하위 1\% 순서 공정성. 부하가 깊어질수록 정책 간 격차가 커지며, 공정성에서 sfqa-auto만 붕괴하지 않는다.}
\label{fig:loadcurve}
\end{figure*}

특히 배치 베이스라인 FGD는 SQUAD의 SFQA와 직교 축의 대조를 선명하게 보여준다. FGD는 단편화 인지 배치만으로 중앙값 대기를 가장 크게 줄였으나(FIFO 대비 54--92\%, 표 \ref{tab:sim}에서 512 단일 113K로 sfqa-auto의 605K보다도 작음), 큐는 FCFS로 유지하므로 과부하에서 하위 1\% 공정성이 0으로 붕괴했다(512 이종 p1=0). 즉 FGD는 ``빠르지만 굶기는'' 효율 축에, sfqa-auto는 ``빠르면서 공정한'' 균형 축에 위치한다. 이는 단편화라는 동일 문제를 배치(FGD)와 큐 우선순위(SFQA)라는 서로 독립적인 축에서 공격할 수 있음을 실증하며, 두 기법을 결합(단편화 인지 배치 + 기아 방지 큐)하는 것이 자연스러운 후속 방향임을 시사한다. (이종 구성의 노드 타입 혼합은 b200/h100/a100 균등 3분할로 정의했으며, FIFO 기준 다른 베이스라인 측정값과 약 0.8\% 이내로 일치한다.)

\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_17.pdf}}
\caption{111k 작업 시뮬레이션. (좌) FIFO 대비 sfqa-auto의 중앙값 대기 단축률, (우) 하위 1\% 순서 공정성(0=완전 기아).}
\label{fig:scale}
\end{figure*}

\begin{table}[htb!]
\centering
\caption{512 GPU 시뮬레이션 발췌 (Philly 111k): 중앙값 대기 / 하위 1\% 공정성}
\begin{tabular}{@{}lcc@{}}
\toprule
\textbf{정책} & \textbf{단일 (1.8$\times$)} & \textbf{이종 (1.8$\times$)} \\ \toprule
FIFO & 1.22M / 100 & 3.52M / 100 \\
SJF & \textbf{643} / 64 & 1066 / \textbf{0} \\
Themis & 1986 / 21 & 3112 / \textbf{0} \\
FGD & 113K / 31 & 521K / \textbf{0} \\
\textbf{sfqa-auto} & 605K / \textbf{54} & 1.77M / \textbf{50} \\
Sia & 1.07M / 63$^{\mathrm{a}}$ & \textbf{200} / 96$^{\mathrm{a}}$ \\ \bottomrule
&&$^{\mathrm{a}}$계산 비용 200$\times$
\end{tabular}
\label{tab:sim}
\end{table}

끝으로, 이 median·공정성 이득이 GPU를 놀려서 얻은 것이 아님을 확인했다(그림 \ref{fig:alloc}). FIFO부터 sfqa-auto까지 거의 모든 정책이 평균 99\% 안팎의 GPU 할당률을 유지한다. 예외는 Sia로, 탄력 ILP가 재구성을 위해 자원을 비워 두어 할당률이 65--73\%에 그친다 --- 즉 Sia의 낮은 큐잉 지연은 일부 GPU를 놀린 대가다. sfqa-auto는 할당률을 떨어뜨리지 않으면서 균형을 달성한다.

\begin{figure*}[htb!]
\centerline{\includegraphics[width=\textwidth]{Pic/Fig_24.pdf}}
\caption{평균 GPU 할당률(Philly 111k, 512 GPU). sfqa-auto를 포함한 대부분의 정책이 약 99\%를 유지하는 반면, Sia는 탄력 재구성으로 인해 65--73\%에 그쳐 낮은 큐잉 지연을 GPU 유휴와 맞바꾼다.}
\label{fig:alloc}
\end{figure*}

\subsection{Lifecycle overhead model}\label{eval:overhead}
이산 사건 시뮬레이터는 작업당 고정 오버헤드(스케줄링·기동·종료)를 주입하여 충실도를 높인다. 본 연구는 전형적인 쿠버네티스 작업 수명 주기 비용에 맞춰 이 값을 약 4--7초(스케줄링 0.5초 + 기동 1.5--3.5초 + 종료 2.5초)로 설정했다. \S\ref{sfqa-auto}의 민감도 분석이 보였듯 결과는 이 보정값의 넓은 범위에 둔감하다.

\section{Conclusions and Future Works}\label{conclu}

본 연구는 GPU 서버 풀에서 HOL 블로킹으로 인한 기아를 해소하기 위한 전처리 메커니즘 SQUAD를 제안하고, 이를 분포별 합성 트레이스 위의 이산 시간 시뮬레이터와 Philly 111k 작업 규모의 이산 사건 시뮬레이터(256--1024 GPU, 단일·이종)로 평가했다. 일반적인 GPU 스케줄러가 사용하는 종래의 접근법과 달리, SQUAD는 코어 스케줄링 정책을 그대로 유지한 채 단편화에서 비롯되는 HOL 블로킹을 해소하여 다양한 스케줄러의 효율을 높인다. SFQA는 most-allocated, compact, 라운드 로빈, MCTS 스케줄러와 함께 적용했을 때 각각 약 14.98\%, 14\%, 7.6\%, 8.9\%의 wall time 단축을 보여 스케줄러 비종속성을 입증했다.

큐잉 이론에 근거해 계수를 제거한 무튜닝 변형 sfqa-auto는 최적 $\alpha$가 워크로드마다 7배 달라지는 문제를 $\sigma^*=1/(1-\rho)$ 유도로 해소한다. 111k 규모 시뮬레이션에서 sfqa-auto는 FIFO 대비 중앙값 대기를 50--61\% 단축하면서 하위 1\% 공정성을 유지하는 유일한 경량 정책이었으며, 다른 빠른 정책들은 과부하·이종 환경에서 공정성이 0으로 붕괴했다.

기존의 SFQA는 최적 계수를 딥러닝의 하이퍼파라미터 튜닝처럼 사후(posterior) 반복 탐색으로 찾아야 한다는 한계가 있었다. 본 연구의 핵심 진전은 바로 이 한계를 sfqa-auto로 해소한 것이다 --- 계수를 사전에 계산해 두는 대신, 측정된 부하로부터 공정성 임계를 실시간으로 유도함으로써 워크로드 변화에 무튜닝으로 적응한다.

다만 sfqa-auto는 큐 순서만을 조정하므로, 백로그가 얕은 일부 상황에서는 기아 작업을 상위로 승급시켜도 작은 작업에 자리를 추월당할 수 있다. 이는 향후 연구에서 감지(무튜닝 긴급도)와 자원 예약(EASY식 자리 잠금, 단 duration 추정 불요)을 결합하여 해소할 계획이다. 그 밖의 가장 중요한 향후 과제는 실제 쿠버네티스 클러스터에서의 대규모 실측으로, 본 시뮬레이션 결과를 하드웨어에서 검증하는 것이다.

\section*{Acknowledgment}
저자들은 본 연구에 대해 어떠한 기관의 재정 지원도 받지 않았다.

%%===========================================================================================%%
%% If you are submitting to one of the Nature Portfolio journals, using the eJP submission   %%
%% system, please include the references within the manuscript file itself. You may do this  %%
%% by copying the reference list from your .bbl file, paste it into the main manuscript .tex %%
%% file, and delete the associated \verb+\bibliography+ commands.                            %%
%%===========================================================================================%%

\bibliographystyle{IEEEtran}
\bibliography{sn-bibliography}
%% if required, the content of .bbl file can be included here once bbl is generated
%%\input sn-article.bbl
\end{document}

